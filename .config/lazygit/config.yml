customCommands:
  - key: 't'
    command: "helix-open {{.SelectedFile.Name | quote}}"
    context: 'files'
    description: 'open in helix'
git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never

#!/usr/bin/env bash
# Delete old home-manager generations and run `nix-collect-garbage -d` to
# reclaim disk space from the Nix store.
set -euo pipefail

# needed?
# sudo rm /nix/var/nix/gcroots/auto/*
rm -f ~/.local/state/nix/profiles/home-manager-*
sudo nix-collect-garbage -d

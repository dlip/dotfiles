#!/usr/bin/env bash
# Bootstrap a per-user Homebrew install at ~/.brew (no sudo required).
# Bails out if ~/.brew already exists.
set -euo pipefail

cd ~

if [ -d .brew ]; then
  echo "Error: Brew directory already exists"
  exit 1
fi

git clone https://github.com/Homebrew/brew .brew

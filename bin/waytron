#!/usr/bin/env bash
# Launch a Chromium/Electron app under Wayland with the right flags
# (UseOzonePlatform / wayland-ime). Usage: waytron <executable>
set -euo pipefail

EXEC=$1
unset PAGER

$1 --enable-features=UseOzonePlatform --ozone-platform=wayland --enable-wayland-ime

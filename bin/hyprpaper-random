#!/usr/bin/env bash
# Cycle hyprpaper through random images from ~/sync/wallpapers every 15 minutes,
# preloading and unloading wallpapers per cycle to keep memory usage bounded.

directory=~/sync/wallpapers
monitor=$(hyprctl monitors | grep Monitor | awk '{print $2}')

if [ -d "$directory" ]; then
	while true; do
		random_background=$(ls $directory/*.{jpg,png} | shuf -n 1)

		hyprctl hyprpaper unload all
		hyprctl hyprpaper preload $random_background
		hyprctl hyprpaper wallpaper "$monitor, $random_background"

		sleep 900
	done
fi

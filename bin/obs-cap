#!/usr/bin/env bash
# Load the v4l2loopback kernel module to expose `/dev/video21` as "OBS Cam",
# letting OBS pretend to be a webcam for use in other apps (Zoom, browser, …).
set -euo pipefail

sudo modprobe v4l2loopback devices=1 video_nr=21 card_label="OBS Cam" exclusive_caps=1

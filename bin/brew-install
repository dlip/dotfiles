#!/usr/bin/env bash
# Install everything declared in ~/Brewfile without upgrading already-installed
# packages. TERMINFO is unset to work around an ncurses build error when run
# from kitty.
set -euo pipefail

# ncurses build error with kitty
unset TERMINFO
brew bundle --force --verbose --no-upgrade --file=~/Brewfile

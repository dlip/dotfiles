#!/usr/bin/env bash
# Walk up to four directory levels under the current directory looking for
# git repositories and append each one to ~/code/projects.txt.
set -euo pipefail

# Project add git folders
find ~+ -maxdepth 4 -name .git -prune | sed 's|/.git$||' | sed "s|${HOME}/||" >> ~/code/projects.txt

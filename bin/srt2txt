#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 1 ]]; then
  echo "Usage: $0 <file.srt>" >&2
  exit 1
fi

sed \
  -e '/^[0-9]\+$/d' \
  -e '/^[0-9][0-9]:[0-9][0-9]:[0-9][0-9],[0-9][0-9][0-9] -->/d' \
  -e '/^$/d' \
  "$1"
